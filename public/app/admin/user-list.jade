.container
    table.table
        tr(ng-repeat="user in users")
            td {{user.firstName + " " + user.lastName}}
            td {{user.id}}
            td {{user.username}}
            td {{user.roles}}